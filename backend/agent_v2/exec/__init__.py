"""V2 execution layer (dry-run scaffolding).

This package will host the real MT5 OrderExecutor and CloseReconciler in
later increments. Increment 3 only ships the dry-run task contract used by
ExecutionQueue.
"""
